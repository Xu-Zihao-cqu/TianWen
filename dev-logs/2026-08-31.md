# 2026-08-31

## 完成事项

- 对 Zynq-7010 的 `BOOT.bin`、`image.ub`、`z7_lite_10.img` 做只读二进制结构分析。
- 识别 BOOT.BIN 的 FSBL、bitstream、U-Boot 分区及偏移/大小。
- 解析 FIT 中 kernel、DTB、ramdisk 的配置、大小与校验值。
- 解析 2 GiB SD 镜像的 MBR、FAT16 BOOT 分区和 ext4 rootfs 分区。
- 确认独立 image.ub 为 initramfs 方案，磁盘镜像内的 image.ub 为 SD/ext4 方案。
- 新增 `docs/Zynq7010-images-reproduction.md`，记录从零功能复现、验证和六类扩展方案。

## 待办事项

- 获取用户实际 Z7-Lite 板卡修订版原理图、XDC、原 Vivado/HLS 工程后，可继续生成可直接执行的 Vivado Tcl、HLS Tcl 和 PetaLinux 配置补丁。
- 在实板上验证 HDMI、PL Ethernet、USB 与两种 RootFS 启动路径。
